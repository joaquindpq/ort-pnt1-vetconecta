@{
    ViewData["Title"] = "Acceso Denegado";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow-lg border-0 card-rounded-custom">
                <div class="card-body p-5 text-center">
                    <div class="access-icon-circle">
                        <i class="fas fa-lock"></i>
                    </div>

                    <h2 class="fw-bold mb-3 text-danger">Acceso Denegado</h2>
                    <p class="text-muted mb-4">No tienes permisos para acceder a esta página</p>

                    <div class="alert alert-warning alert-no-border">
                        <i class="fas fa-exclamation-triangle me-2"></i>
                        Esta sección requiere autenticación o permisos especiales que tu cuenta no posee actualmente.
                    </div>

                    <div class="d-grid gap-3 mt-4">
                        <a asp-controller="Home" asp-action="Index" 
                           class="btn btn-outline-secondary btn-lg btn-pill btn-split-left">
                            <i class="fas fa-home me-2"></i>Volver al Inicio
                        </a>
                        <a asp-controller="Account" asp-action="Login" 
                           class="btn btn-primary btn-lg btn-pill" >
                            <i class="fas fa-sign-in-alt me-2"></i>Iniciar Sesi&oacute;n
                        </a>
                    </div>

                    <p class="mt-4 text-muted">
                        ¿Necesitas ayuda? 
                        <a href="/contacto" class="text-decoration-none fw-semibold text-link-primary">
                            Contáctanos
                        </a>
                    </p>
                </div>
            </div>
        </div>
    </div>
</div>
